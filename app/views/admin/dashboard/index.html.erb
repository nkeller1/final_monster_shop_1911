<h2>All Orders</h2>

<% @orders.each do |order| %>
  <section id="orders-<%=order.id%>">
    <%= order.id %>
    <%= order.user.name %>
    <%= order.created_at.strftime("%_m/%e/%C") %>
    <%= order.status %>
  </section>
<% end %>
